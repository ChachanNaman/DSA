// Terminal nodes are those with outdegree = 0 
// So, Firstly reverse the egdes to get the terminal node 
// and terminal nodes are the safe nodes already
// the nodes with indegree 0 now are terminal nodes (coz edges reversed)